import reducer from './reducer';

export { default as feedTypes } from './types';
export { default as feedActions } from './actions';
export { default as feedSelectors } from './selectors';

export default reducer;
