import reducer from './reducer';

export { default as signupTypes } from './types';
export { default as signupActions } from './actions';
export { default as signupSelectors } from './selectors';

export default reducer;
